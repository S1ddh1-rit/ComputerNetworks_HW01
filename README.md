# Packet Sniffer (pktsniffer)

This program analyzes packets from a pcap file and displays
Ethernet, IP, and transport-layer (TCP/UDP/ICMP) headers.

It uses the Scapy library to read and process packet data.
